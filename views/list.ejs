<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>Document</title>
</head>
<body>
    <div class="box" id="heading">
        <h1><%= title %></h1>
    </div>
    
    <div class="box">
        <%itemsArray.forEach(function(e){%>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= e._id %>" onchange="this.form.submit()">
                    <p><%= e.name %></p>
                </div>
                <input type="hidden" name="listName" value="<%= title %>"></input>
            </form>
        <% }) %>
    
    <form class="item" action="/" method="post">
        <input type="text" name="newitem" placeholder="What do you plan to do?" autocomplete="off">
        <button type="submit" name="list", value="<%=title%>">+</button>
    </form>
    </div>

    
    
</body>
</html>